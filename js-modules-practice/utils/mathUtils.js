export function add(a,b){ // Exporting indiividual functions (named export)
    return a+b;
}

export function subtract(a,b){
    return a-b;
}

const pi = 3.142;
export default pi; // exporting a single main item (defaulf export)